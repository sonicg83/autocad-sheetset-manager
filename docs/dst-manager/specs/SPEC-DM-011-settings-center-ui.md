---
id: SPEC-DM-011
title: 设置中心 UI 设计规范（齿轮入口 + 模态对话框 + 关于页）
status: draft
owners:
  - dst-manager
created: 2026-09-08
updated: 2026-09-08
related:
  - ARCH-DM-004
  - GUIDE-DM-001
  - SPEC-DM-006
  - PRD-DM-001
document_kind: spec
---

# 设置中心 UI 设计规范

> 状态：草稿（G4 Demo 待用户操作确认与冻结；G6 起进入实施计划）
> 定位：设置中心前端子项目的权威交互 Spec。架构、API 契约、存储与校验语义的权威是 [ARCH-DM-004](../architecture/ARCH-DM-004-settings-center.md)，本文只引用不复制；视觉基准来源为 [设计冻结包](#7-设计冻结包)（含[交互 Demo](../mockups/SPEC-DM-011-settings-demo.html)）。门禁方法依据 [GUIDE-DM-001](../guides/GUIDE-DM-001-frontend-design-implementation-gates.md)。

## 1. G0 立项与范围

- **一句话**：让用户在不加载 DST、不编辑 `.env` 的前提下，通过桌面界面查看、修改应用配置并了解软件版本与协议。
- **变更等级**：L 级——触碰共享外壳（顶部栏）、后端契约（`/api/settings`、`/api/about`）、桌面壳（ShellBridge 新增原生选择器/外链）、发布链路（PyInstaller spec 数据文件）。已拆分为两个可独立验收的子项目：
  - **子项目 A（配置域后端）**：resolver / store / 快照 / Worker 传播 / API / 打包触点。纯后端，GUIDE-DM-001 不适用，按 ARCH-DM-004 §7 测试策略验收。
  - **子项目 B（本 Spec）**：设置中心 UI，走 G0～G9 全流程。
- **范围**：顶部齿轮入口、设置模态对话框（常规配置 + 关于）、动态表单渲染、原生路径选择器桥接、来源/覆盖标记、外链、诊断横幅。
- **非目标**：扩展设置本体与扩展中心（PRD-DM-001 后续立项）；`data_dir`/`draft_dir` 界面化；模板目录；第三方组件声明页；主题或全局导航改动。
- **权威来源**：业务行为 → ARCH-DM-004；交互与状态 → 本文；视觉 → 设计冻结包；实施顺序 → 后续 Plan。
- **同主题检索**：无既有 Spec/Plan/Demo 覆盖设置中心；PRD-DM-001 仅定义扩展平台预留边界。

## 2. G1 业务目标

- **主要用户**：使用 DST Manager 桌面软件的单机用户（非开发者，不应被要求编辑 `.env`）。
- **核心用户任务**（按重要性）：
  1. 配置 AutoCAD 版本路径（首次使用的前置任务）；
  2. 调整任务执行参数（超时/并发/租约）；
  3. 切换编号规则选项；
  4. 查看版本号、开源协议与反馈入口。
- **可观察成功标准**：不加载 DST 即可完成上述全部任务；每项修改保存后无需重启即对下一次操作生效；误输入在保存前得到行内提示；用户可把任一项恢复为继承值。
- **不可变约束**：服务只监听 `127.0.0.1`；配置语义权威为 Pydantic Settings（ARCH-DM-004 §2.1）；凭据永不进入设置。
- **数据描述**：典型 9 个配置项、4 个分组、2 个分区；极端为超长路径（>200 字符）、含中文与空格的路径、被网络盘断开的路径。

## 3. G2 用户流程与状态矩阵

### 3.1 流程

- **入口**：顶部栏齿轮（常驻，含未加载工作区态）→ 打开设置对话框。
- **主路径**：选择分区 → 修改字段 → 即时校验 → 保存（PUT）→ 已保存反馈 → 继续或关闭。
- **取消路径**：无修改时 Esc/✕/遮罩直接关闭；有修改时确认（放弃/留在此处）后关闭。
- **失败恢复路径**：校验失败 → 行内错误 + 焦点定位，输入保留；保存失败（写入/权限）→ 错误横幅，内存值不替换，用户可重试或关闭；配置文件损坏 → 诊断横幅 + 已回退默认提示；Schema 过新 → 只读诊断态。
- **导航关系**：对话框悬浮于任何页面之上；关闭后焦点归还齿轮；不改变底层页面状态（工作区、草稿、任务浮层不受影响）。

### 3.2 状态矩阵

| 类别 | 状态 |
| --- | --- |
| 生命周期 | 初始（GET 加载中）、加载成功、加载失败（重试）、空（无诊断） |
| 编辑 | 未修改、编辑中、校验失败（逐字段）、待保存、保存中、保存成功（短暂提示）、保存失败 |
| 数据 | 短/长路径、缺失值（`null`）、来源 default/env/file、有覆盖/无覆盖、损坏文件、Schema 过新 |
| 导航 | 未加载工作区打开、已加载工作区打开、分区切换、关闭后焦点归还、重复打开 |
| 视图 | 浅色、深色、最小支持视口、200% 缩放、面板内滚动 |
| 权限与能力 | 桌面壳（桥可用 → 浏览按钮可用）、浏览器开发态（浏览禁用、手输保留）、外链能力 |

### 3.3 关键状态规则

- 保存采用"全有或全无"（ARCH-DM-004 §3）；失败时逐字段显示后端错误，输入保留。
- 编辑期间后端值被外部改变（本设计下仅另一进程 `serve` 可能）→ 保存返回 409，对话框刷新快照并提示重新确认（输入保留）。
- `enable_add_number_suffix`、`number_suffix_type`、`cad_max_parallel` 保存成功后显示"已保存，相关预览将按新配置重算"提示（SC-13）。

## 4. 需求条目（追踪矩阵锚点）

| ID | 要求 | 权威依据 |
| --- | --- | --- |
| SC-01 | 顶部栏齿轮按钮常驻可见（含未加载工作区），点击打开设置对话框 | ARCH-DM-004 §4.1 |
| SC-02 | 对话框左右结构：左分区列表（常规配置/关于），右内容面板；超高面板内滚动 | ARCH-DM-004 §4.2 |
| SC-03 | 表单按注册表描述动态渲染：category 分组、控件映射（path/bool/int/enum） | ARCH-DM-004 §4.2 |
| SC-04 | path 字段：浏览（EXE/DLL 过滤器按 `file_filter`）+ 清除（nullable）+ 手输；浏览器开发态浏览禁用 | ARCH-DM-004 §2.1、§4.3 |
| SC-05 | 每字段显示来源标记（默认/环境变量/用户配置）与覆盖标记；提供"恢复继承"操作 | ARCH-DM-004 §3 |
| SC-06 | 即时校验：类型/范围行内错误；保存失败时焦点落到第一个错误字段 | ARCH-DM-004 §4.4、§5 |
| SC-07 | 保存状态机：保存中（禁用按钮）→ 成功（短暂提示）/ 失败（逐字段错误横幅） | ARCH-DM-004 §4.4 |
| SC-08 | 未保存修改时 Esc/✕/遮罩关闭先确认，不静默丢弃 | ARCH-DM-004 §4.4 |
| SC-09 | 键盘：Esc 关闭、焦点圈闭、关闭后焦点归还齿轮、错误聚焦 | ARCH-DM-004 §4.4 |
| SC-10 | 浅/深主题、最小支持视口、200% 缩放下不破版、不横向溢出 | GUIDE-DM-001 G8 |
| SC-11 | 关于分区：应用名+版本号、MIT 协议全文（面板内滚动）、主页/反馈链接经系统浏览器打开（仅登记 https） | ARCH-DM-004 §4.2 |
| SC-12 | 诊断横幅：损坏已回退默认、Schema 过新只读态；只读态禁用保存 | ARCH-DM-004 §5 |
| SC-13 | 编号规则/并发保存成功后提示"相关预览将按新配置重算" | ARCH-DM-004 §4.4 |
| SC-14 | 对话框打开时拖放事件不穿透遮罩；不干扰底层页面拖拽功能 | ARCH-DM-004 §4.4 |

## 5. G3 视觉方向裁决

- **比较过的方向**：
  1. **独立工作区标签页**——否决：设置是全局能力，标签栏在未加载 DST 时不出现，无法满足"未加载即可配置"；且稀释业务标签语义。
  2. **顶部齿轮 + 模态对话框（选定）**——用户裁决（2026-09-07，ASCII 草图确认）：入口常驻、形态与既有确认模态一致。
- **沿用基准**：SPEC-DM-006 设计令牌（色彩/圆角/阴影/间距/焦点环）、既有 `<dialog>` 模态模式（焦点圈闭 + `::backdrop` + 焦点归还）、表单行内错误模式（SPEC-DM-010 属性页 `.property.error` + `.field-error`）、徽章状态语言。
- 无新增全局 CSS 规则；对话框样式限定于组件作用域。

## 6. G5 技术映射

| 设计要求 | 当前实现 | 差距 | 数据/接口 | 修改位置 | 风险 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| SC-01 齿轮常驻 | 顶部栏 `App.vue` 顶部动作区（主题切换等） | 无齿轮入口 | — | `App.vue` 顶部栏 + 新增入口组件 | App.vue 已超大，只加挂载点不加逻辑 | e2e 未加载态可见 |
| SC-02/03 对话框与动态渲染 | 既有确认模态组件 | 无设置对话框 | `GET /api/settings` `items[]` | 新建 `components/settings/SettingsDialog.vue` + `composables/useSettings.ts` | 动态渲染字段控件映射 | e2e + 单测快照 |
| SC-04 浏览/清除 | ShellBridge 文件拖拽、Explorer 桥 | 无 `select_folder`/`select_file` | pywebview `create_file_dialog` | `interfaces/shell.py` ShellBridge 新方法 | frozen 态真实弹窗需 G9 手工验收 | 单测（桥协议）+ G9 |
| SC-05 来源标记 | 无 | 无 | `items[].source/has_file_override` | `useSettings` 状态 + 渲染徽章 | — | e2e |
| SC-07 保存 | 既有 PUT 模式（如属性保存） | 新端点 | `PUT /api/settings`（`set`/`unset`/`expected_revision`） | api 客户端 + 对话框保存逻辑 | 409/422 逐字段路径 | 集成测试 + e2e |
| SC-09 键盘 | 既有模态焦点管理模式 | 对话框级焦点圈闭 | — | 复用确认模态模式 | 与 document 级拖拽桥兼容（SC-14） | e2e 键盘用例 |
| SC-11 关于页 | 无 | 无 | `GET /api/about` | 关于分区组件 | frozen 态版本元数据（ARCH-DM-004 §8） | 集成测试 + G9 |
| SC-12 诊断 | 无 | 无 | `diagnostics[]` | 横幅组件 | — | e2e |
| SC-13 预览重算提示 | 既有 preview digest 机制 | 无 | — | 保存成功回调内条件提示 | — | e2e |

- **预期 Demo 与生产差异**：Demo 不调用真实 API（模拟 `items` 快照）；"浏览…"用模拟原生选择器；外链点击仅提示；版本号为虚构值；"恢复继承"在 Demo 中即时改内存展示，生产语义为保存时经 `unset` 提交（ARCH-DM-004 §3）。差异表随设计冻结包归档。
- **禁止修改**：全局样式、主题令牌、ActionDock、任务浮层、既有页面组件。

## 7. 设计冻结包

- [交互 Demo（SPEC-DM-011）](../mockups/SPEC-DM-011-settings-demo.html)——冻结版本：commit `a570986` + 分区高亮重置与诊断横幅配色两处修正（见截图索引同批提交）；用户已操作确认（2026-09-08）。
- 关键状态 × 视口 × 主题截图索引（`assets/SPEC-DM-011/`，桌面基准 1280×720，最小视口 900×600）：

| 文件 | 状态 | 主题 | 视口 |
| --- | --- | --- | --- |
| `g4-01-general-light.png` | 常规配置默认态（来源徽章/浏览/清除/恢复继承） | 浅色 | 基准 |
| `g4-02-validation-error-light.png` | 编辑中 + 即时校验行内错误 | 浅色 | 基准 |
| `g4-03-saved-light.png` | 保存成功（修订号递增、已保存提示） | 浅色 | 基准 |
| `g4-04-general-dark.png` | 常规配置 | 深色 | 基准 |
| `g4-05-about-dark.png` | 关于分区（版本/MIT/外链） | 深色 | 基准 |
| `g4-06-corrupt-min-viewport-light.png` | 损坏文件诊断横幅（amber）、来源回退默认 | 浅色 | 最小 |

- 组件与令牌表：沿用 SPEC-DM-006 令牌，无新增。
- 交互与状态规则：本文 §3；Demo 模拟限制见 §6 差异表。
- 非目标见 §1。

## 8. 门禁记录

| 门禁 | 状态 | 证据链接 | 确认人 | 日期 | 未关闭事项/恢复条件 |
| --- | --- | --- | --- | --- | --- |
| G0 立项与范围 | 通过 | 本文 §1；ARCH-DM-004 §1 | 用户 | 2026-09-08 | — |
| G1 业务目标 | 通过 | 本文 §2；ARCH-DM-004 §1.2 | 用户 | 2026-09-07 讨论 | — |
| G2 流程与状态 | 通过 | 本文 §3；状态规则经草图与讨论确认 | 用户 | 2026-09-07 | — |
| G3 视觉方向 | 通过 | 本文 §5（用户选定齿轮+对话框） | 用户 | 2026-09-07 | — |
| G4 Demo 与设计冻结 | 通过 | [Demo](../mockups/SPEC-DM-011-settings-demo.html) + `assets/SPEC-DM-011/` 截图索引；用户操作确认 | 用户 | 2026-09-08 | 冻结后 Demo 再变更须重开本门禁 |
| G5 技术映射 | 通过 | 本文 §6 | 技术负责人（Agent） | 2026-09-08 | — |
| G6 计划就绪 | 通过 | [PLAN-DM-019](../../../.planning/plans/dst-manager/PLAN-DM-019-settings-center.md) 创建（含 SC-01～SC-14 与 A-01～A-07 追踪矩阵、全局约束、12 任务四批次） | 技术负责人（Agent） | 2026-09-08 | — |
| G7 分批实施 | 通过 | PLAN-DM-019「实际验证」：批次 1–4（Task 1–11）全部实施并评审收口；`uv run ruff check .` 全绿、全量 pytest 0 失败、`npm run build` 通过、Playwright e2e 全量通过（数字见 Plan） | 技术负责人（Agent） | 2026-09-08 | G8 截图比对、G9 真实桌面验收未开始 |
| G8 设计 QA | 未开始 | — | — | — | 生产实现 vs `assets/SPEC-DM-011/` 冻结截图同状态比对（默认/校验失败/保存/深色/诊断/最小视口） |
| G9 真实验收与关闭 | 未开始 | — | — | — | 桌面壳路径选择器真实弹窗、外链系统浏览器、frozen 版本/LICENSE 读取、保存后真实 CAD 任务生效需真实环境 |

## 9. 验收标准（G8/G9 锚点）

- 追踪矩阵每条 SC-xx 达到"已验证"；G8 用同状态截图对比 Demo 与生产（默认/编辑/校验失败/诊断/浅深主题/最小视口）。
- G9 必须在打包后桌面壳验证：齿轮入口、原生路径选择器真实弹窗（EXE/DLL 过滤器）、保存后配置对真实 CAD 任务生效、外链系统浏览器打开、关于页版本与 LICENSE 读取。
- e2e 必须覆盖：未加载工作区打开、修改→保存→重开保留、行内校验失败与焦点、未保存关闭确认、来源标记与恢复继承。
