# ARCH-DM-005 多语言实施门禁待办

日期：2026-09-09

状态：已立项；G0～G6 已完成，G7～G9 待实施

关联：`ARCH-DM-005`、`SPEC-DM-013`、`PLAN-DM-021`、`PLAN-DM-020`

## 目的

在 `PLAN-DM-020` Task 10 开始前，完成 DST Manager 全局多语言能力的正式设计与实施计划，提供唯一 `vue-i18n` 实例、`zh-CN`/`en-US` 资源装载、`ui_locale` 设置、稳定错误 key 渲染和原生对话框本地化协议。不得为图纸目录单独建立临时翻译器。

## 安排

1. 在 PLAN-DM-020 批次一至批次二执行期间启动该工作流，不晚于批次二检查点。
2. 依据 `GUIDE-DM-001` 完成 G0～G3，明确全应用迁移范围、分阶段兼容边界和中英文布局基准。
3. 产出可操作 Demo 与双语关键状态截图，用户确认后关闭 G4。
4. 核对现有设置中心、启动挂载、ShellBridge、错误契约、OpenAPI 与全部 Vue 文案落点，关闭 G5。
5. 已建立 `SPEC-DM-013`、追踪矩阵和 `PLAN-DM-021`（`proposed`），G6 已关闭；正式 Plan 状态及 ID 已回填到 `PLAN-DM-020`“实际验证”。
6. 多语言基础实现并通过其自动化、设计 QA 和必要桌面验收后，才允许开始 PLAN-DM-020 Task 10。

## 完成条件

- 有已接受的多语言 Spec 和冻结设计证据；
- 有独立的 `PLAN-DM-*` 实施计划及明确批次；
- 生产代码中存在唯一 i18n 实例和两语言资源加载能力；
- 中英文 key 集合一致性、启动无闪烁、即时切换、设置保存失败保持原语言和 ShellBridge 固定类型本地化均有自动测试；
- PLAN-DM-020 记录正式 Plan ID 与验证状态，不再依赖未命名前置工作。
