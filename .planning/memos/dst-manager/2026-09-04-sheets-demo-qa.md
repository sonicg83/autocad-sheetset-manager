# 图纸页交互 Demo 验证记录

日期：2026-09-04。对应 [SPEC-DM-009](../../../docs/dst-manager/specs/SPEC-DM-009-sheets-workspace-ui.md) 与 [交互 Demo](../../../docs/dst-manager/mockups/SPEC-DM-009-sheets-demo.html)。仅用于本轮设计确认，不是产品实施验收。

## 范围与视觉核对

- 独立单文件 HTML，13 张虚构图纸、5 个子集、12 项图纸自定义属性；无需安装依赖，不请求业务 API。
- 对照会话确认的效果图上半部分，检查了左树右单表、白色卡片、浅灰底色、蓝色选中态、行内编辑、固定底部操作区。新增列配置、直接删除、6 项属性分页和完整任务入口来自后续已确认的 SPEC，属于有意差异；不是对生成图逐像素复刻。
- 检查 1440×900 浅色桌面与 900×800 深色窄屏。窄屏文档宽度与视口均为 900px，无整页横向溢出；表格保留独立横向滚动，导航收起为按钮。
- 已修正删除文字被链接样式覆盖为蓝色、默认列宽过大两个显示问题。截图人工核对未见阻断操作的重叠；长文件名以截断和提示处理。
- 证据保存在本地会话可视化目录 `sheets-demo-desktop.png`、`sheets-demo-narrow.png`，未将截图或真实客户信息加入仓库。视口测试后恢复默认尺寸。

## 实际交互验证

- 全部范围显示 13 张；选择二期子集显示 3 张，不自动勾选。
- 编辑 009：第一页修改图幅，第二页修改校核人，返回第一页输入保留；点击新增图纸触发未提交输入保护，选择加入草稿后继续，两项均提交。
- 在 009 前插入 2 张：总数变为 15，二期子集变为 5，原 009 保持对象与属性并重编号为 011。
- 显示列取消出图比例、启用布局，表头即时变化；刷新后偏好保留；恢复默认有效。
- 删除图纸加入草稿后数量减少；撤销恢复，重做再次移除。
- 模拟发布要求勾选确认，完成后清空草稿并建立新模拟基准。复现并修正预览数量写死问题；重复操作后显示“基准 12 张 → 当前 11 张”。
- 新建子集分别选择基础模板和布局模板，提交后插入参照子集之后，导航与图号更新。
- 跨范围选择 001 与 008，单子集显示“已选 2 张，其中 1 张不在当前结果”；批量清空备注要求再次确认，确认后两张备注均清空。
- 浏览器警告/错误日志为空。交付前重置模拟数据与草稿，保留二期子集展示，便于开始试用。

## 自动检查

- `node --test tests/demo/sheets-demo.test.cjs`：7/7 通过，覆盖范围、属性提交、参照插入、非法参照与数量、删除快照、显式清空、新子集模板要求。
- `uv run pytest tests/unit/test_core.py -q`：退出码 0；有既有 Alembic `path_separator` 弃用警告。
- `uv run ruff check .`：当次退出码 1，其他工作区改动 `src/dst_manager/runtime.py:25` 存在 B009；未修改该文件。
- `git diff --check`：通过。

## 明确限制

- 文件夹打开、模板选择、预览、发布和修订历史均为模拟，不执行文件读写、CAD、备份或发布安全校验。
- 属性标签仅给出范围提示；属性页交互应由 SPEC-DM-010 单独评审。
- 草稿仅保存于页面会话，刷新会重置；列偏好保存于当前浏览器。模拟名称派生、单次数量上限 20 和属性值长度上限 200 不是正式业务规则。
- 自动测试针对演示数据模型；浏览器验证为本次实际人工编排操作，不宣称生产 Playwright 套件、完整键盘/屏幕阅读器验收或真实 CAD 验收通过。未改 `web/`，不执行生产构建和生产 E2E。

结论：本轮交互演示可供用户功能评审，正式实施状态不变。
