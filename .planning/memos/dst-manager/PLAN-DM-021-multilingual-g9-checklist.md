---
id: MEMO-DM-026
title: PLAN-DM-021 G9 真实桌面验收清单——打包 WebView2 壳多语言逐项记录
status: draft
owners:
  - dst-manager
created: 2026-09-09
updated: 2026-09-09
related:
  - PLAN-DM-021
  - SPEC-DM-013
  - ARCH-DM-005
  - MEMO-DM-025
---

# PLAN-DM-021 G9 真实桌面验收清单（MEMO-DM-026）

> **性质：** 待执行记录单。G9（真实 pywebview/WebView2 桌面壳验收，SPEC-DM-013 §7.2）无法由实施代理在浏览器/自动化环境中替代执行（计划全局约束："无真实环境时计划保持 `active`"），本清单由实施代理按 Task 12 Step 4 准备，全部结果字段留空，由操作者在真实 Windows 桌面逐项填写。**在所有字段填写并复核前，PLAN-DM-021 保持 `active`。**

## 1. 执行环境登记（操作者填写）

| 项 | 值 |
| --- | --- |
| 操作者 | _待填写_ |
| 执行日期 | _待填写_ |
| Windows 版本与区域 | _待填写_（如 Windows 11 企业版 26200，区域 zh-CN） |
| 系统（显示）语言列表与优先级 | _待填写_（`Get-WinUserLanguageList` 输出） |
| WebView2 Runtime 版本 | _待填写_（`msedgewebview2.exe` 文件版本） |
| 被测分发包 | _待填写_（`dist/releases/dst-manager-v0.3.0-win64.zip` 的 SHA-256） |
| 被测 commit | _待填写_（应为 `main` 合入前的 `feat/dm-021-multilingual` 最终 commit） |
| pyproject 版本 | _待填写_（应为 0.3.0；`/api/about` 与顶栏显示一致） |
| 是否使用去敏工程副本 | _待填写_（建议：复制 `sample/project2` 至 `%TEMP%` 后打开副本） |

## 2. 逐项验证记录

每项记录：结果（通过 / 缺陷 + 编号）与截图路径。任何一项缺陷即按计划"批次四失败"处理：计划保持 `active`，修复后重新取证。

### 2.1 中文显示语言

- 系统语言 zh-CN 启动：界面、设置中心、错误 Toast、确认框全中文；`<html lang>` 为 `zh-CN`。
- 结果：_待填写_；截图：_待填写_；系统语言：_待填写_；备注：_待填写_。

### 2.2 英文显示语言

- 系统语言切换为 en-US 重启：全英文；列宽/按钮伸长不截断；`<html lang>` 为 `en-US`。
- 结果：_待填写_；截图：_待填写_；备注：_待填写_。

### 2.3 非中英显示语言

- 系统语言为其他语言（建议 ja-JP 与 fr-FR 各一次）：按 I18N-03 映射 `en-US`；不可识别语言环境回退 `zh-CN`。
- 结果：_待填写_；截图：_待填写_；实际语言：_待填写_；备注：_待填写_。

### 2.4 显式设置覆盖

- 设置中心显式选择语言（如 en-US）保存后，系统语言改为另一语言再重启：显式值仍生效；`settings.json` 的 `ui_locale` 出现文件覆盖标记。
- 结果：_待填写_；截图：_待填写_；备注：_待填写_。

### 2.5 设置读取失败降级

- 将 `settings.json` 置为损坏 JSON（或以只读目录模拟读取失败）后启动：应用按系统语言规则正常挂载，不阻断、不闪现错误语言整屏；设置中心呈现读取失败提示。
- 结果：_待填写_；截图：_待填写_；实际行为：_待填写_。

### 2.6 保存成功 / 失败事务

- 保存成功：语言即时切换一次，焦点归还保存按钮，背景输入与选区保留。
- 保存失败（422 / 409 / 拔网线或停后端）：对话框保持、语言不切换、本地输入保留、错误摘要可聚焦。
- 结果：_待填写_；截图：_待填写_；备注：_待填写_。

### 2.7 原生文件对话框过滤器与取消

- 分别触发 `dst`、`template`、`exe`、`dll` 四类选择（前两类在工作区入口，后两类在设置中心"浏览"）：过滤器扩展名恒定（`(*.dst)`、`(*.dwt)`、`(*.exe)`、`(*.dll)`），中英文只改变描述文字；"取消"返回 null 且界面无错误。
- 结果：_待填写_；截图：_待填写_（四类 × 中英可合并）；备注：_待填写_。

### 2.8 英文窄屏 / 200% 缩放

- en-US 下 900×768（深色）与 200% 缩放：无整页横滚、主操作可达、表格只在自身容器横滚、长错误可读；Tab/Esc 焦点圈闭与归还。
- 结果：_待填写_；截图：_待填写_；备注：_待填写_。

### 2.9 发布与任务不中断

- 语言切换前后启动发布/导入任务：SSE 状态码稳定、任务面板中英文渲染一致、任务不被语言切换中断或重建。
- 结果：_待填写_；截图：_待填写_；备注：_待填写_。

## 3. I18N-16 物理 hash 复验（操作者填写）

自动化层证据（Task 12 已完成，见计划"实际验证"）：对去敏副本 `sample/project2`（1 个 DST + 5 个 DWG）以真实后端打开工作区并执行 `ui_locale` zh-CN→en-US→zh-CN 设置事务，全部文件 SHA-256 与 mtime_ns 零变化；`settings.json` 仅落在隔离目录，工程目录内无 settings 写入。

G9 需在打包壳重复物理验证：

| 项 | 值 |
| --- | --- |
| 副本来源与脱敏说明 | _待填写_ |
| 切换前 hash+mtime 快照 | _待填写_（附脚本输出或截图） |
| 中→英切换后 | _待填写_ |
| 英→中切换后 | _待填写_ |
| 结论（一致 / 差异+缺陷编号） | _待填写_ |

## 4. G9 裁决

- 结论：_待填写_（通过 / 部分通过 + 遗留缺陷清单）。
- 操作者签名/日期：_待填写_。
- 通过后动作：由用户确认后将 PLAN-DM-021 `status` 改为 `completed`，并同步 SPEC-DM-013 §8 门禁表与本文 `status` → `final`。

## 5. 与 G9 一并留给用户裁决的遗留项

1. **D3 文案裁决（G8 收口）**：生产设置项英文标签为 **UI language**（G5 冻结键 `settings.items.uiLocale`），Demo 为 **Display language**。语义等价；若需逐字对齐 Demo，需同步中英资源与后端 `label_key`。记录于 MEMO-DM-025，待用户裁决后 G8 才登记通过。
2. **草稿旧版 label 兼容回退（I18N-17 迁移窗口）**：`web/src/components/DraftActionsPanel.vue` 渲染期保留 `action.label` 兜底展示（仅旧版本草稿含本地化 label 文本时触发；新草稿一律持久化 `label_key` + `params`）。计划 Task 12 未要求在本任务删除；窗口结束（旧草稿不再需要兼容）时删除该回退并补全仓扫描。
