---
id: MEMO-DM-030
title: PLAN-DM-020 G8 用户真实桌面复验与原结论撤销记录
status: final
document_kind: memo
owners:
  - dst-manager
created: 2026-09-11
updated: 2026-09-11
related:
  - PLAN-DM-020
  - PLAN-DM-023
  - SPEC-DM-012
  - MEMO-DM-027
  - MEMO-DM-028
---

# PLAN-DM-020 G8 用户真实桌面复验与原结论撤销记录

## 1. 复验范围与证据边界

- 复验日期：2026-09-11。
- 复验人：用户（业务负责人）。
- 对照基准：SPEC-DM-012 §7.4 已冻结的 G4 Demo（commit `9f3dfb3`）及 `default-light-1440x1000.jpg`。
- 生产证据：用户提供的真实 Windows 桌面完整截图与首屏截图；截图含真实工程显示信息，按私有资料处理，不复制到公开仓库或自动化夹具。
- 本次仅裁决页面视觉、信息层级和主任务操作流；没有重新验证原生另存为、Explorer、Excel 或 Artifact 行为，G9 仍未执行。
- 两侧数据行数和输出列数不同，不把业务数据内容本身计为视觉偏差；只比较稳定布局、控件形态、信息层级、滚动和操作可达性。

## 2. 用户裁决

用户明确反馈：除输出列的上移、下移、删除由文字按钮改成符号按钮这一差异可保留外，生产页面整体明显偏离已认可的 G4 Demo，不能接受 MEMO-DM-027 将 D1～D10 归为“已接受差异”并据此登记 G8 通过。

据此作出以下正式裁决：

1. MEMO-DM-027 的 G8“通过”结论自 2026-09-11 起失效；SPEC-DM-012 §16 的 G8 恢复为“未通过（用户真实桌面复验）”。
2. G4 设计冻结继续有效，不重开 G4。当前目标是把生产实现拉回既有冻结件；只有用户未来决定采用另一套布局时才重开 G3/G4。
3. G9 暂停执行。先完成 PLAN-DM-023 并由用户重新通过 G8，再使用 MEMO-DM-028 执行真实桌面功能验收。
4. PLAN-DM-020 保持 `active`，不得在 G8/G9 重新通过前标记 `completed`。

## 3. 偏差重新分类

| ID | 当前生产表现 | 与冻结基准的关系 | 裁决 |
| --- | --- | --- | --- |
| V1 | 输出列由紧凑表格式改为逐列大卡片，重复显示“输出列名 N / 表达式 N” | 改变核心编辑结构并显著增加页面高度 | G8 阻断缺陷 |
| V2 | 兼容性摘要成为独立全宽卡片，与具体列及输出列状态分离 | 改变信息层级，产生空白和跨区理解成本 | G8 阻断缺陷 |
| V3 | 预览与导出被推到长页面下方，普通首屏无法形成“编辑→即时预览→导出”闭环 | 改变冻结 Demo 的核心任务节奏 | G8 阻断缺陷 |
| V4 | 字段浏览器取消搜索，字段引用语法只放在 `title`，页面只常显属性名称芯片 | 弱化可发现性与学习反馈，不等价于 Demo 的双行条目 | G8 缺陷 |
| V5 | 缺少列状态、`N / 50 列`计数和表头层级 | 丢失冻结件的容量与校验反馈 | G8 缺陷 |
| V6 | 可用状态仍常显版本、生命周期和大块管理提示，模板栏与正文之间增加整行状态卡 | 冗余信息挤压主任务 | G8 缺陷；非 AVAILABLE/失败态仍须保留可见诊断 |
| V7 | 900px 下字段浏览器保持自然高度，输出列、兼容性、预览和导出继续下移 | 未匹配冻结件的字段区限高降级 | G8 缺陷 |
| V8 | 超宽窗口下编辑器无有效阅读宽度和列宽约束，表达式框被过度拉长 | 扫描距离过大、信息密度失衡 | PLAN-DM-023 同步收敛 |
| A1 | 上移、下移、删除使用 `↑`、`↓`、`✕`，并保留完整 `aria-label` | 比文字按钮更紧凑，不改变行为和可访问名 | 用户接受，继续保留 |
| A2 | 真实生产壳的顶栏、右缘任务入口和底部 ActionDock | Demo 的评审脚手架未完整复现生产壳 | 接受为壳层差异；内容区仍必须按冻结件收敛 |

## 4. 原 G8 过程问题

- 实施侧把“功能存在、语义大致相同”替代为“符合冻结视觉”，错误接受了表格到卡片、可见语法到 tooltip、首屏预览到滚动后可达等结构变化。
- `sheet-catalog-visual-evidence.spec.ts` 只守住无横向溢出和滚动后可达，并在测试注释中预先把首屏偏差写成“已接受差异”；该测试不能作为冻结设计一致性的充分证据。
- MEMO-DM-027 没有为 D1～D10 记录业务负责人的逐项确认人和日期，却仍把 G8 登记为通过，违反 GUIDE-DM-001 G8 的差异裁决要求。
- “要求向 Demo 收敛应重开 G4”的表述方向错误：冻结件未变时应修生产实现；只有选择新方向才重开 G4。

## 5. 恢复条件

G8 只有同时满足以下条件才能重新登记为通过：

1. PLAN-DM-023 的生产改动和回归测试完成。
2. 1440×1000 浅色、900×700 深色使用相同虚构数据、滚动位置和展开状态生成成对截图。
3. 生产证据截图进入 `docs/dst-manager/specs/assets/SPEC-DM-012/production/`，不再只保存于被忽略的 `.superpowers/`。
4. V1～V8 均有同状态截图和几何/可见正文证据；A1、A2 作为仅有的预先接受差异保留。
5. 用户逐对确认并记录日期；在用户确认前不得由实施代理自行写“已接受差异”或“G8 通过”。

## 6. PLAN-DM-023 实施完成与 G8 逐对比对记录（2026-09-11，待用户复核）

### 6.1 实施与证据

- 生产改动：`441b85c`（红灯）→ `1cf0a8f` → `0d69e45` → `70b23c0` → `241c7b5`；详见 [PLAN-DM-023 实际验证](../../plans/dst-manager/PLAN-DM-023-sheet-catalog-visual-convergence.md)。
- 生产证据（入版本库）：[`production/g8-catalog-light-1440x1000.png`](../../../docs/dst-manager/specs/assets/SPEC-DM-012/production/g8-catalog-light-1440x1000.png)（1440×1000，浅色，缺值警告态）与 [`production/g8-catalog-dark-900x700.png`](../../../docs/dst-manager/specs/assets/SPEC-DM-012/production/g8-catalog-dark-900x700.png)（900×700，深色），尺寸与冻结基准图一致。
- 同口径虚构数据：4 列「市政标准目录」、5 张图纸（003/004 缺「设计人」）、初始滚动位置 0、两主题默认展开态。

### 6.2 几何对照（同一数据与状态，实测值）

| 区域 | 冻结 Demo 1440×1000 | 生产 1440×1000 | 冻结 Demo 900×700 | 生产 900×700 |
| --- | --- | --- | --- | --- |
| 页头 | 48px（两行） | 24px（单行） | 48px | 24px |
| 模板栏 | 85px | 50px | 85px | 50px |
| 工作区栅格 | 443px（字段 258px 宽） | 425px（字段 258px 宽） | 425px | 713px（单列） |
| 字段区可见高度 | 443px，列表内部滚动 | 425px，列表内部滚动 | 2px（被页面 flex 压缩，内容溢出） | 235px，列表内部滚动 |
| 输出列卡 | 443px | 425px | 425px | 466px |
| 兼容性状态带 | 66px（两行） | 42px（一行） | 66px | 42px |
| 列区（含表头） | 236px 可视 / 322px 内容 | 287px 可视 / 328px 内容 | 218px / 322px | 328px / 328px |
| 数据行高 | 80px | 74px | 80px | 74px |
| 卡片标题字（列） | 34px | 32px | 34px | 32px |
| 预览卡 | 250px | 259px | 250px | 250px |
| 导出按钮底缘 | 969px（Demo 内 981px 容器底） | 914px（ActionDock 顶 948px） | 947px | 滚动后可达 |
| 页面滚动 | 无（内容恰好填满） | 无（`scrollHeight == clientHeight`） | 有 | 有 |

### 6.3 追踪矩阵逐项结论（实施代理判定，非用户裁决）

| ID | 判定 | 依据 |
| --- | --- | --- |
| V1 | 一致 | 唯一 `.columns-head`（顺序/列名/表达式/状态/操作）与每行五列共用同一 grid 轨道；E2E 实测列边界误差 0px（≤ 1px）；列区内部滚动。 |
| V2 | 一致 | 「兼容性摘要」region 是「输出列编辑器」region 的后代；输出列卡头显示「可以导出，有 1 项提示」徽标；警告态允许导出、错误态禁用导出且正文与焦点行为不变（既有 a11y 用例全过）。 |
| V3 | 一致 | 1440×1000 初始滚动位置下预览标题、第三行数据与「导出 XLSX」均在同一首屏且不被 ActionDock 遮挡（914px < 948px）。 |
| V4 | 一致 | 「搜索可用字段」输入框可见；条目两行：第一行常显规范引用（`sheet.number`、`sheetset["项目.编号"]`），第二行用户名称（图号/图名/文件名或规范属性名）。 |
| V5 | 一致 | 卡头 `4 / 50 列`；状态列逐行文字「有效」，未知字段行转「需修正」且错误正文在表达式单元格内。 |
| V6 | 一致 | 可用态无独立 `.catalog-status` 大卡（页头 24px）；模板栏恢复「内置/已保存模板」与「已保存/有未保存修改」两类状态文字；版本/生命周期与启停指引仍为可见正文（`extensions-navigation.spec.ts` 的防停用回退用例继续通过）。 |
| V7 | 一致 | 六列模板下预览位置相对四列位移 ≤ 2px；列区自身可滚动；900×700 单列下字段区限高 235px 且内部滚动，输出列紧随其后。 |
| V8 | 一致 | 1920×1080 下字段浏览器保持 258px；列轨道 34 / 66 / 92 / 112px，表达式轨道宽于列名轨道，无无轨道的整片空白。 |
| A1 | 保留 | `↑`/`↓`/`✕` 图标按钮 + 完整 `aria-label`；操作轨道因此为 112px（Demo 文字按钮为 188px）。用户已接受差异。 |
| A2 | 保留 | 生产壳顶栏 52px + 标签栏 42px + 底部 ActionDock 52px；页面占满壳层剩余高度，只接受壳层差异。用户已接受差异。 |

### 6.4 与冻结基准的可见差异（待用户裁决）

以下三项**未被实施代理接受**，作为差异候选提交用户裁决：

1. **卡片标题文案**：生产为「字段浏览器 / 输出列编辑器 / 预览」（SPEC-DM-012 §7.2 区域名称，同时是 region 可访问名），冻结 Demo 为「可用字段 / 输出列 / 结果预览」。PLAN-DM-023 未要求修改标题文案，且 V1～V8 未涵盖该项，故本次保持原样。
2. **页头多出两段可见正文**：生产单行页头额外显示 `v0.1.0 · 可用` 与「扩展的启用与停用请在设置中心操作。」；冻结 Demo 只有标题与说明。两者分别是 SPEC-DM-012 §7.3（页面状态矩阵）与 SPEC-DM-011 修订「启停交互改进」要求保留的可见诊断，且受 `extensions-navigation.spec.ts` 用例钉住，不能仅因对齐截图而移除。
3. **900×700 字段区高度**：冻结截图里字段卡为 2px（Demo 被页面 flex 压缩、内容溢出的自身缺陷），生产按本计划 Task 5 步骤 2 实现为 235px 限高 + 内部滚动。本项是**有意不复制 Demo 缺陷**，需用户确认。

另有四项纯尺寸差异（页头 48→24px、模板栏 85→50px、栅格 443→425px、兼容带 66→42px、数据行 80→74px），均属同一冻结结构下的密度压缩：生产壳层占地大于 Demo 评审脚手架，内容区用密度与内部滚动适配（本计划全局约束）；未改变控件形态、信息层级或操作可达性，也不改变主流程。

### 6.5 门禁状态（用户确认前）

- SPEC-DM-012 §16 的 G8 记为「待用户复核（PLAN-DM-023 已实施）」。
- PLAN-DM-023 保持 `active`；G9 保持暂停（不解除）。
- 本轮未登记为“G8 通过”，未填写用户确认人与日期，未把任何差异写成“已接受”。

