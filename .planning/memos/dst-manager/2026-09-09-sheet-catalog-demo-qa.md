# 图纸目录 G4 Demo 自动化 QA 记录

日期：2026-09-09

范围：`SPEC-DM-012` 的单文件交互 Demo。自动化与视觉预检先行完成，用户随后于 2026-09-09 确认认可，G4 已据此通过并冻结。

## 验证对象

- [图纸目录交互 Demo](../../../docs/dst-manager/mockups/SPEC-DM-012-sheet-catalog-demo.html)
- 冻结版本：commit `9f3dfb3`。
- 冻结截图：[`1440 × 1000` 浅色](../../../docs/dst-manager/specs/assets/SPEC-DM-012/default-light-1440x1000.jpg)、[`900 × 700` 深色](../../../docs/dst-manager/specs/assets/SPEC-DM-012/default-dark-900x700.jpg)。
- 视觉方向：用户已选择的“表达式模板”布局。
- 浏览器：Codex 应用内浏览器。

## 已验证行为

- 正常模板显示缺值数量警告，但允许预览和导出。
- 不兼容模板明确显示 `sheet.建筑专业` 缺失并禁止导出；修正表达式后恢复导出。
- 未保存修改切换模板时显示“另存为模板、放弃修改、留在此处”；留在此处保持原模板和输入。
- 内置默认模板固定为图号、图名、文件名三列。
- 命名模板另存后立即进入模板列表；删除用户模板后回到内置默认模板。
- 字段从浏览器插入当前表达式光标位置；特殊名称使用 `{sheetset["项目.编号"]}`，预览正确求值。
- 空图纸集允许进入模拟另存为流程；取消不产生成功提示。
- 模拟原生另存为显示固定 XLSX 目标，完成后只显示最终路径，不显示 Artifact、修订或哈希。
- 浅色桌面视口 `1440 × 1000` 和深色最小视口 `900 × 700` 均无页面横向溢出；单栏下 4 行编辑控件均具有可见高度。
- 最终干净页面浏览器控制台 `error/warn` 为 0；内嵌脚本通过 Node `vm.Script` 语法检查。

## 迭代修正

1. 修复首轮脚本结束花括号缺失导致的空页面。
2. 修复最小视口单栏布局压缩输出列编辑区的问题。
3. 将字段插入从“追加到末尾”修正为“插入当前光标位置”，并补充方括号特殊属性示例。
4. 修复取消模板切换后下拉框显示与实际模板不一致的问题。

## Demo 与生产差异

| Demo | 生产要求 |
| --- | --- |
| 使用 5 张虚构图纸和内存模板 | 使用宿主提供的不可变工作区快照及 SQLite 模板仓储 |
| 表达式解析只覆盖演示语法 | 后端实现 `SPEC-DM-012` 完整受限语法和结构化 token |
| 原生另存为由两个虚构路径模拟 | ShellBridge 打开系统另存为并签发一次性保存授权 |
| 不创建 XLSX、不登记 Artifact | 候选 XLSX 回读校验、宿主原子保存后登记后台 Artifact |
| 场景下拉框直接切换状态 | 生产状态由真实 API、修订和扩展生命周期驱动 |

## 结论

自动化预检通过，用户于 2026-09-09 明确认可。G4 状态更新为“通过”；后续若改变主流程、布局结构或关键状态，必须重新打开 G4，不得用实施偏差静默改写冻结设计。
